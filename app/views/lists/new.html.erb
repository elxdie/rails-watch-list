<%# create a form box %>
<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">
        <h3>Create a new list</h3>
        <%= simple_form_for @list do |f| %>
        <%# placeholder writes an example in the box which is temporary%>
          <%= f.input :name, placeholder: 'List type' %>
          <%= f.submit class: 'btn btn-flat' %>
        <% end %>
      </div>
    </div>
  </div>
</div>


<%# <h1>New Movie</h1>

<%= simple_form_for(@list) do |f| %>
  <%# <%= f.input :name %>
  <%# <%= f.submit %>
<%# <% end %>
